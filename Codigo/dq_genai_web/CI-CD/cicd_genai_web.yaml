steps:
  - name: 'node:20'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        npm install -g firebase-tools && cd ../public && firebase init && firebase deploy --only hosting:tfg-generador-de-reglas
