steps:
  - name: 'node:20'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        npm install -g firebase-tools && cd Codigo/dq_genai_web/public && firebase deploy --project tfg-dq --only hosting:tfg-generador-de-reglas
